{
	"name": "es-experiments",
	"version": "1.0.0",
	"description": "es2015 and es2016 experimenting",
	"main": "index.js",
	"scripts": {
		"deps": "cp node_modules/babel-polyfill/dist/polyfill.min.js lib/vendor/babel-polyfill.min.js",
		"transpile": "babel src -d lib",
		"lint": "node_modules/.bin/eslint src",
		"build": "npm run lint -s && npm run deps -s && npm run transpile -s"
	},
	"author": "David Vujic",
	"license": "MIT",
	"devDependencies": {
		"babel-cli": "^6.5.1",
		"babel-eslint": "^4.1.8",
		"babel-preset-es2015": "^6.5.0",
		"babel-preset-stage-1": "^6.5.0",
		"babel-preset-stage-2": "^6.5.0",
		"babel-preset-stage-3": "^6.5.0",
		"eslint": "^2.1.0"
	},
	"dependencies": {
		"babel-polyfill": "^6.5.0"
	}
}
